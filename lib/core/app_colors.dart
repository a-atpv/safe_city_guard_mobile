import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const Color background = Color(0xFF0B1026);
  static const Color surface = Color(0xFF121B3A);
  static const Color cardDark = Color(0xFF0F1832);
  static const Color cardLight = Color(0xFF162040);
  static const Color bottomBar = Color(0xFF0D1530);

  // Accent
  static const Color accent = Color(0xFF2ECC71);
  static const Color accentDark = Color(0xFF27AE60);
  static const Color accentLight = Color(0xFF3DDB85);

  // Status
  static const Color danger = Color(0xFFE74C3C);
  static const Color warning = Color(0xFFF39C12);
  static const Color info = Color(0xFF3498DB);

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF8E99B7);
  static const Color textHint = Color(0xFF5A6584);

  // Input
  static const Color inputBackground = Color(0xFF162040);
  static const Color inputBorder = Color(0xFF253261);

  // Divider
  static const Color divider = Color(0xFF1E2A4A);

  // Shield logo colors
  static const Color shieldOuter = Color(0xFFD4A843);
  static const Color shieldInner = Color(0xFF0B1026);
}
